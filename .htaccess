<IfModule mod_rewrite.c>
    RewriteEngine On

    # Redirect www ke non-www
    RewriteCond %{HTTP_HOST} ^www\.sinergiflowindo\.com [NC]
    RewriteRule ^(.*)$ https://sinergiflowindo.com/$1 [L,R=301]

    # Rewrite ke folder public
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
